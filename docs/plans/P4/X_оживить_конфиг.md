# Промпт X — оживить конфиг (вне плана Ф4)

**Не пакет Ф4.** Отдельная работа, найденная 2026-09-04 при разборе
`config/routing.yml`. Приоритет высокий: касается ~18 баллов по двум жюри.

## Обоснование

ТЗ, техжюри, критерий 2 «Гибкость правил маршрутизации» (32 балла):
- **6** — параметры правил можно изменять через настройки или конфигурацию без
  изменения основной логики;
- **5** — можно добавлять новые правила и провайдеров без переработки архитектуры.

ТЗ, эксперты, критерий 2 «Гибкость маршрутизации» (33 балла):
- **7** — решение не привязано к конкретному набору входных данных и допускает
  изменение параметров провайдеров и правил.

**Замер (менял значение, сравнивал md5 вывода):**

```
эталон:                  268edee7411b
outcomes.seed 42 -> 777: 268edee7411b   НЕ влияет
calibrate_from_history:  268edee7411b   НЕ влияет
outcomes.source:         268edee7411b   НЕ влияет
allocator.tie_break:     268edee7411b   НЕ влияет
rate_limits vipay 7->1:  268edee7411b   НЕ влияет
obligations min -> 9M:   268edee7411b   НЕ влияет
fallback_provider:       4d1426775ba6   влияет
strategy (контроль):     3d3a76a11773   влияет
```

Из восьми секций работают три. Судья, меняющий `rate_limits` и видящий тот же
вывод, снимет не только 6 баллов, но и доверие к остальному.

```
Проект smart-routing-42, /home/vmelnik/RubymineProjects/smart-routing-42.
Задача: оживить config/routing.yml — половина секций сегодня декоративна.

## Замер, воспроизводящий проблему

Меняешь значение ключа, прогоняешь bin/route, сравниваешь md5 выходного
routing_decisions_test.json с эталонным. Не влияют: outcomes.seed,
outcomes.source, outcomes.calibrate_from_history, allocator.tie_break,
rate_limits, obligations. Влияют только strategy, amount_ranges,
fallback_provider (и layers как проверка запуска).

## Что сделать — три вещи, в этом порядке

1. ОЖИВИТЬ outcomes (это снятый как опциональный пакет W3b из
   docs/plans/PHASE_3_VOVA.md, строки 728–759). Ключи seed, source,
   calibrate_from_history обязаны влиять на прогон. Приоритет источников уже
   построен в CFG-2 и менять его нельзя: CLI > YAML > дефолт. Сейчас
   default_options в bin/route перекрывают YAML — это и есть баг.
   Приёмка: смена seed в YAML меняет вывод; --seed поверх YAML перекрывает его.

2. allocator.tie_break — ЛИБО протянуть до аллокатора, ЛИБО удалить ключ из
   конфига и схемы. Держать ключ, который обещает управление детерминированным
   tie-break, но не управляет им, нельзя. Реши сам и обоснуй в отчёте; если
   протягиваешь — детерминизм обязан сохраниться, make determinism зелёный.

3. obligations и rate_limits — НЕ УДАЛЯТЬ и не «чинить». Полей
   daily_turnover_min/max и requests_per_minute_limit нет в
   reference/data/providers.json (противоречие C-3, принят вариант A: семантику
   допуска не меняем). Вместо этого запуск должен САМ говорить об этом на
   stderr, например:

     конфиг: rate_limits заданы для vipay, quickpay — в снапшоте провайдеров
             нет поля requests_per_minute_limit, правило не применяется

   Комментарий в шапке YAML не считается: на защите его никто не читает.
   Предупреждение печатается один раз при загрузке конфига, не на каждую
   операцию, и не ломает прогон. Спек на текст предупреждения обязателен.

## Границы

- НЕ выносить в конфиг пороги hard-constraints. Они приходят из снапшота
  организаторов, и подмена их конфигом сломает инвариант «selected_provider
  обязан входить в множество допустимых по исходному снапшоту».
- lib/config/ — зона Кирилла (CFG-1). Правки схемы допустимы, но опиши их в
  отчёте отдельным пунктом: их надо с ним согласовать.
- Боевой config/routing.yml остаётся с strategy: count_share, layers: [].

## Приёмка

Повтори замер из шапки этого файла после правки и приложи таблицу. Каждая
секция обязана либо влиять на вывод, либо печатать причину, почему не влияет.
make gate, make validate (29/0), make determinism — все зелёные.
```

Дальше — общая часть из `00_ОБЩИЙ_КОНТЕКСТ.md`.
