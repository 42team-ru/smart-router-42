# Промпт W5 — гейт зоны и правка документов

Последний пакет фазы. Запускать, когда W2, W3, W4 приняты.

```
Проект smart-routing-42, /home/vmelnik/RubymineProjects/smart-routing-42.
Фаза Ф4. Делаешь пакет W5 — гейт зоны и приведение документов в соответствие
с кодом.

## Бриф

docs/plans/PHASE_4_VOVA.md, «БРИФ W5» (строки 989–1015) и §7 «Гейт зоны Вовы
в Ф4» — полный чеклист приёмки.

## Что делаешь

1. docs/ARCHITECTURE.md
   - §8 «Слои»: описать конвейер как он есть в коде — стратегия даёт базовый
     порядок, слои образуют лексикографический кортеж отклонений поверх неё,
     первый слой в списке старший. Имена слоёв: budget_headroom, share_ceiling.
   - §9: пометить, что observed_conversion_below: 0.5 — иллюстрация синтаксиса;
     перечислить четыре реально реализованных целочисленных предиката.
   - §14: заменить layers: [conversion, load, obligations] на реальный пример.
     ЭТО ВАЖНО: имена conversion/load/obligations заняты СТРАТЕГИЯМИ, и такой
     конфиг сегодня уронит запуск. Добавить фразу «имена слоёв намеренно не
     совпадают с именами стратегий: реестры раздельные».

2. README.md — раздел про слои и селектор: команды прогона трёх примеров
   конфигов, формула psi, ссылка на AdWords (Mehta et al., FOCS 2005), честная
   пометка что боевой конфиг идёт с layers: [] и почему (C-3 плана — включение
   psi двигает целевое распределение, решение человека).

3. scripts/check_determinism.sh — второе правило: в lib/routing/layers запрещены
   \.to_f\b, Float\(, Math\. . ОТСУТСТВИЕ КАТАЛОГА НЕ ПОВОД МОЛЧА ПРОЙТИ —
   скрипт должен падать, если каталога нет, иначе правило тихо отключится.

## Критерий приёмки

Полный список §7 плана, все пункты зелёные. Плюс: прогони все три примера
конфигов через reference/scripts/validate_10.rb, каждый «Ошибок: 0».
```

Дальше — общая часть из `00_ОБЩИЙ_КОНТЕКСТ.md`.
