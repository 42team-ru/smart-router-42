# Общий контекст для всех промптов Ф4

Этот блок вставлен в каждый промпт `Wn_*.md` — здесь он лежит отдельно, чтобы
править в одном месте. Если правишь — прогони по всем файлам каталога.

## Постоянная часть

```
Проект smart-routing-42, /home/vmelnik/RubymineProjects/smart-routing-42.
Фаза Ф4, задачи X-1/X-2/X-3 из docs/TASKS.md.

## Бриф

Твоя постановка — docs/plans/PHASE_4_VOVA.md. Прочитай целиком, отступать от
замороженных контрактов §3 нельзя. Пакеты, которые не твои, не трогай.

## Инварианты (CLAUDE.md, читай целиком)

- Детерминизм: rand/shuffle/sample/Time.now запрещены в lib/routing и
  lib/execution, ловится гейтом. Любой Dir[...] с .sort.
  ВАЖНО: хук форматирования (rubocop -A на записи файла) уже однажды молча
  удалил .sort из решающего пути. Если пишешь такое — пиши и спек на наличие
  конструкции в исходнике, иначе правка уедет при зелёном гейте.
- Целочисленная арифметика в решающем пути, дроби сравниваются перекрёстным
  умножением, веса в базисных пунктах. Ни Float, ни to_f, ни Math.* в
  lib/routing/layers.
- Причины отсева дословно из reference/data/reference_decisions.json.
- decision в attempts только selected/skipped. Формат attempts заморожен после
  гейта этапа 2: всё новое едет в существующие поля details сегментами через
  " | ", в каждом сегменте число. Причина без числа не принимается.
- Слой переупорядочивает допущенных, но НЕ РАСШИРЯЕТ множество. Кирилл будет
  ломать это property-тестом T-5 — не оставляй дыр.
- Онлайн-обработка: слой видит только текущую операцию и состояние. Заглядывать
  в будущие операции очереди запрещено.
- selected_provider обязан входить в множество допустимых по ИСХОДНОМУ снапшоту.

## Определение готовности

  make gate       # rspec + rubocop + детерминизм
  make validate   # валидатор организаторов, обязан остаться 29/0
  make determinism

Дефолтный --out-dir у bin/route это out/, а не корень репозитория.
Боевой config/routing.yml оставь с strategy: count_share и layers: [] —
включение слоёв в бою решает человек отдельно (см. C-3 плана).
Рабочее дерево оставь чистым от временных файлов. Коммит не делай.

## Отчёт

Что сделал по пакету, хвосты make gate и make validate, все числа приёмки
прогоном, и ОТДЕЛЬНО — всё, где отступил от брифа или где бриф оказался неверен.
Если твой прогон расходится с числами плана — остановись и скажи, не подгоняй
спек под результат.
```

## Состояние репозитория (обновляй перед запуском)

На 2026-09-04, после W0/W1:

- `make gate` — 116 файлов rubocop чисто, 0 падений. `make validate` — 29/0.
- Готово: Ф0–Ф3, R-4 (дневной лимит по живому состоянию), W0 (ключи конфига),
  W1 (`Layers::Base`, `LayerStack`, `PlanTrace`, врезка в `Planner`).
- `lib/routing/layers/` содержит только `base.rb` — слоёв ноль.
- Проверено: с `layers: []` вывод не изменился ни на байт по всем семи
  стратегиям (diff пустой).
